apiVersion: v1
kind: Service
metadata:
  name: {{ include "gp-rancher-logging.fullname" . }}
  labels:
    {{- include "gp-rancher-logging.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gp-rancher-logging.selectorLabels" . | nindent 4 }}
