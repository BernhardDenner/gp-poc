apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gp-rancher-logging.fullname" . }}-test-connection"
  labels:
    {{- include "gp-rancher-logging.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gp-rancher-logging.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
